#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int t;
    cin >> t;

    while (t--) {
        int x;
        cin >> x;
        // office is X km away from the home
        int result = x * 2 * 5;
        cout << result << endl;
    }
    return 0;
}
